package com.squirtles.feature.map

internal enum class BottomNavigationSize(
    val size: Int
) {
    WIDTH(245),
    HEIGHT(50),
    HORIZONTAL_PADDING(32)
}

internal enum class BottomNavigationIconSize(
    val size: Int,
) {
    CENTER(82),
    CENTER_ICON(34)
}
